# TODO - Modern Forest Villa (Next.js)

- [ ] Create Next.js + Tailwind + Motion + Lenis project scaffold (package.json, next.config.ts, tsconfig.json, postcss.config.js, tailwind.config.ts)
- [ ] Create app router files: app/layout.tsx, app/page.tsx, app/globals.css
- [ ] Create required components (Navbar, FullscreenMenu, LenisProvider, Preloader, SequenceScroll, etc.)
- [ ] Move/copy existing sequence frames into Next public path: public/sequence/ (keeping existing sequence folder intact)
- [ ] Implement scroll-linked canvas image sequence (HTML5 Canvas) with FPS-stable requestAnimationFrame drawing and high-DPI support
- [ ] Add cinematic overlays, grain/vignette, and sequence progress indicator
- [ ] Implement magnetic CTA button and navbar menu overlay
- [ ] Wire everything together and ensure `npm install` + `npm run dev` works
